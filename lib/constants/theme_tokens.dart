import 'package:flutter/material.dart';

class ShellTokens {
  ShellTokens._();

    static const Color chromeBase = Color(0xFF0F0F0F);
    static const Color chromeSurface = Color(0xFF171717);
    static const Color chromeBorder = Color(0xFF242424);

    static const Color accent = Color(0xFF3ECF8E);
    static const Color accentMuted = Color(0xFF1A4A3C);

    static const Color textPrimary = Color(0xFFE4E4E4);
    static const Color textSecondary = Color(0xFF8E8E8E);
    static const Color textDisabled = Color(0xFF545454);
}

class SemanticTokens {
  SemanticTokens._();

  static const Color success = Color(0xFF3ECF8E);
  static const Color warning = Color(0xFFD1943C);
  static const Color error = Color(0xFFC2483D);
}

class ContentTokens {
  ContentTokens._();

    static const Color background = Color(0xFF0F0F0F);
    static const Color card = Color(0xFF171717);
    static const Color border = Color(0xFF242424);

  static const Color textPrimary = Color(0xFFE4E4E4);
  static const Color textSecondary = Color(0xFF8E8E8E);
  static const Color textDisabled = Color(0xFF545454);
}