import 'package:flutter/material.dart';

class ShellTokens {
  ShellTokens._();

    static const Color chromeBase = Color(0xFF0B0D09);
    static const Color chromeSurface = Color(0xFF12140F);
    static const Color chromeBorder = Color(0xFF1E201A);

    static const Color accent = Color(0xFF5B8C5A);
    static const Color accentMuted = Color(0xFF3D5E3C);

    static const Color textPrimary = Color(0xFFE8EBE4);
    static const Color textSecondary = Color(0xFF989C94);
    static const Color textDisabled = Color(0xFF5B5F58);
}

class SemanticTokens {
  SemanticTokens._();

  static const Color success = Color(0xFF4B8B4A);
  static const Color warning = Color(0xFFC2823A);
  static const Color error = Color(0xFFC2483D);
}

class ContentTokens {
  ContentTokens._();

    static const Color background = Color(0xFF0E100C);
    static const Color card = Color(0xFF131510);
    static const Color border = Color(0xFF1E201A);

  static const Color textPrimary = Color(0xFFE8EBE4);
  static const Color textSecondary = Color(0xFF989C94);
  static const Color textDisabled = Color(0xFF5B5F58);
}
